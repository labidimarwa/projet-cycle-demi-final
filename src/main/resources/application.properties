# Server Configuration

server.port=8080
server.servlet.context-path=/api/v1

# Database Configuration
spring.datasource.url=jdbc:mariadb://localhost:3306/nexgenai_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=


spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# Logging
logging.level.com.nexgenai=DEBUG
logging.level.org.springframework.security=INFO


# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=marwalabidi255@gmail.com
spring.mail.password=slow xqfp hhiu kqqn
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true




app.cv.upload-dir=uploads/cv

# ─── Microservice Python (extraction CV + embeddings JobBERTa) ────────────────
matching.python.url=http://127.0.0.1:8000
matching.python.timeout=600000


matching.python.health.retries=10
matching.python.health.retry-delay-ms=5000
logging.level.reactor.core.publisher.Operators=OFF

# ─── Seuils de similarité cosinus ────────────────────────────────────────────
matching.similarity.threshold.match=0.75
matching.similarity.threshold.partial=0.50

# ─── Seuils de recommandation (score global) ─────────────────────────────────
matching.recommendation.retenir=75
matching.recommendation.etudier=50






spring.task.execution.pool.core-size=2
spring.task.execution.pool.max-size=4
spring.jpa.open-in-view=false





# =============================================
# CODE EXECUTION CONFIGURATION
# =============================================

# Timeout per test case in seconds
app.code-execution.timeout-seconds=10

# Memory limit per Docker container in MB
app.code-execution.memory-mb=128

# Use Docker sandbox (set false for local dev without Docker)
app.code-execution.use-docker=true

# =============================================
# APPLICATION CONFIGURATION
# =============================================

# Base URL for image links
app.base-url=http://localhost:8080

# Image upload directory
app.question.image-dir=uploads/questions

# =============================================
# SPRING CONFIGURATION
# =============================================

# Multipart file upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB